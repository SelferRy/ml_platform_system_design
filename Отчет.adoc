== ДЗ 1
2025.03.19

. (+) Сформированы функциональные требования (10 доменов, 40+ FR)
. (+) Сформированы нефункциональные требования (производительность, доступность, масштабируемость, безопасность, данные)
. (+) Определены риски (15 рисков с митигациями)
. (+) Определены границы MVP и backlog
. (+) Спроектирована концептуальная архитектура (микросервисы + event-driven)
. (+) Определены пользователи решения (7 ролей)
. (+) Описаны внешние интеграции (7 систем)
. (+) Описаны потоки данных (синхронный инференс, развёртывание, batch)
. (+) Спроектирована модель данных (7 сущностей, DDL)
. (+) Выполнены расчёты показателей (хранение, throughput)
. (+) Описана система хранения данных (PostgreSQL, S3, Redis, Kafka, Prometheus)
. (*) Учтены замечания рецензента (PR #1): разделение ФТ/НФТ, бизнес-клиент, gateway без логики, данные моделей

2025.04.03 — Закрыт тех.долг по замечаниям рецензента (PR #2)

. (+) Добавлен FR-1005 (data residency enforcement per tenant) в домен "Глобальная инфраструктура"
. (+) Добавлены NFR-107 (throughput batch worker ≥10K/≥500 записей/сек), NFR-108 (SLA batch: ≤30 мин light / ≤2 ч heavy), NFR-208 (стратегия восстановления batch при сбое)
. (+) Границы MVP перестроены на 3 фазы: Baseline/Alpha → MVP → Future; добавлено обоснование MVP-среза с принципами отбора
. (+) В модель данных добавлена сущность Tenant (id, name, plan, quota, region, status); tenant_id добавлен в Model, User, APIKey
. (+) В схемы данных добавлен DDL таблицы tenants и tenant_id с FK и индексами в трёх таблицах
. (+) В расчёты показателей добавлены: сетевая нагрузка inference и batch S3, PostgreSQL IOPS, обоснование числа инстансов со ссылками на NFR

== ДЗ 2
2025.04.04

. (+) Определены узкие места и стратегия масштабирования (горизонтальное/вертикальное, обоснование по компонентам) — doc/2.07
. (+) Добавлены пороги развития архитектуры: условия перехода к шардированию PG, multi-cluster, multi-region — doc/2.07
. (+) Описан подход к фазам проектирования: Baseline с extension points, рассмотрены альтернативы — doc/2.08
. (+) Детализирована схема архитектуры с HA: replica sets (min/max реплики, AZ-распределение), auto-scaling HPA конфигурация, LB health checks (readiness/liveness/startup probes), Circuit Breaker — doc/2.08
. (+) Создана HA-диаграмма с трёхзонным развёртыванием — diagrams/ha-deployment.puml → PNG
. (+) Создан раздел Observability: стек (Prometheus/VictoriaMetrics/Loki/Grafana/Alertmanager), метрики inference/batch/model/system, SLO с error budget, правила алертов P0–P3, capacity planning через метрики — doc/2.09
. (+) Создана диаграмма observability стека — diagrams/observability.puml → PNG
. (+) Создан раздел "Реагирование на инциденты": матрица приоритетов P0–P3, runbooks для 5 сценариев (inference down, высокая латентность, PostgreSQL failover, Kafka failure, batch job завис), DR-план для 3 сценариев (AZ failure, region failure, data corruption), chaos engineering план, postmortem процесс — doc/2.10
. (+) Раскомментированы include для 2.09 и 2.10 в architecture-decision.adoc
. (+) Каналы алертинга абстрагированы от конкретного мессенджера (Telegram/Slack/MS Teams — на выбор) — doc/2.09, doc/2.10, diagrams/observability.puml

== ДЗ 3
2025.04.05

. (+) Обновлена диаграмма контейнеров: добавлены WAF (AWS WAF), явный Load Balancer (NLB/ALB), Service Discovery (Kubernetes DNS), Kafka раскрыта до трёх явных топиков (deploy-events, batch-jobs, inference-logs) с consumer groups — diagrams/c4-container.puml → PNG
. (+) Создан раздел "Схема взаимодействия": принципы sync vs async (таблица с обоснованием по 7 типам операций), расчёт технической нагрузки (декомпозиция 1000 RPS бизнес → ~3150 RPS Redis + ~150 RPS GPU), описание трёх sequence-диаграмм — doc/2.11
. (+) Создана sequence-диаграмма синхронного инференса: WAF → ALB → API Gateway → Redis auth → rate limit → Inference Service → Redis cache (hit/miss) → GPU/Triton → Kafka fire-and-forget; показаны Circuit Breaker alt и cache hit alt — diagrams/seq-inference-sync.puml → PNG
. (+) Создана sequence-диаграмма развёртывания модели (event-driven FSM): 202 Accepted → Kafka (deploy-events) → Deployment Service → Kubernetes API → health check loop → FSM переходы; показан автооткат при failure — diagrams/seq-model-deploy.puml → PNG
. (+) Создана sequence-диаграмма async batch inference: submit → Kafka (batch-jobs) → KEDA Batch Workers → S3 → polling статуса; показаны DLQ при persistent failure и retry с exponential backoff — diagrams/seq-batch-inference.puml → PNG
. (+) Создан раздел "Форматы взаимодействия": таблица протоколов (REST/gRPC/Kafka/SQL) с обоснованием, Service Discovery (K8s DNS, соглашение именования), WAF правила (OWASP Top 10, Managed Rule Groups), Rate Limiting (Token Bucket, обоснование vs альтернативы), версионирование API (/v1/, /v2/) — doc/2.12
. (+) Добавлены OpenAPI-контракты для 4 ключевых эндпоинтов: POST /v1/models, POST /v1/models/{id}/deploy, POST /v1/infer, GET /v1/models/{id} — doc/2.12
. (+) Добавлены Protobuf-схемы: InferenceService.proto (Predict, HealthCheck) и events.proto (DeploymentRequested, DeploymentSucceeded, DeploymentFailed, InferenceLogged) — doc/2.12
. (+) Раскомментированы include для 2.11 и 2.12 в architecture-decision.adoc


== ДЗ 4
2025.04.05

. (+) Добавлен прогноз роста данных по годам (Y1/Y3/Y5) для S3, PostgreSQL, Kafka, VictoriaMetrics, inference logs — doc/2.07
. (+) Добавлена классификация данных по температуре (hot/warm/cold) и характеристикам (статичные/модифицируемые), read/write ratio по компонентам, hot/cold split с влиянием на стоимость — doc/2.07
. (+) Переработан раздел "Система хранения данных": добавлены CAP-анализ (CP для PG, AP для Redis/Kafka/S3/VictoriaMetrics с обоснованием), стратегия шардирования (Baseline без шардирования + порог и стратегия для MVP), HA и репликация по каждому хранилищу (PG Multi-AZ, Redis Sentinel, Kafka RF=3/min.ISR=2, S3 versioning) — doc/2.13
. (+) Добавлены CQRS два уровня: Redis read path для инференса (cache hit без PG) и PostgreSQL read replica для UI/API; materialized view mv_active_models на CQRS read model — doc/2.13
. (+) Описан CDC через Debezium: WAL → Kafka топики pg-cdc-model-versions, pg-cdc-deployments; обоснование (decoupling, надёжность, replay); пометка "extension point для MVP" — doc/2.13
. (+) Описан CDN слой: CloudFront для статики веб-консоли (JS/CSS/HTML); Cache-Control immutable; инвалидация при деплое UI; явно указано что inference/API/артефакты через CDN не идут — doc/2.13
. (+) Добавлены Backup таблица (PG RDS snapshot + WAL archiving RPO<5мин, S3 versioning, Redis RDB, Kafka retention, VictoriaMetrics → S3) и таблица ограничений по хранилищам — doc/2.13
. (+) Добавлены Partition Management: SQL-функции создания ежемесячных партиций и автоматического удаления старых с экспортом в S3 — doc/2.14
. (+) Добавлены ключевые индексы с обоснованием (8 индексов: B-tree composite, Hash для api_keys, обоснование vs BRIN) — doc/2.14
. (+) Добавлены CQRS Read Models: materialized view mv_active_models (production-модели с endpoint) + trigger для REFRESH CONCURRENTLY; mv_tenant_usage (агрегация ресурсов per tenant) — doc/2.14
. (+) Детализирован CQRS паттерн в таблице паттернов: два уровня + read replica; добавлен CDC паттерн как самостоятельная строка; добавлены CDN и Debezium в таблицу ключевых компонентов — doc/2.02
. (+) Обновлена диаграмма контейнеров: добавлены CDN (CloudFront), S3 Static bucket, Debezium connector, Kafka CDC топики (pg-cdc-model-versions, pg-cdc-deployments) — diagrams/c4-container.puml → PNG
. (+) Добавлен раздел CDN-слой в схему архитектуры: поток запроса к UI, обоснование применения только для статики, механизм инвалидации; обновлена таблица коммуникации компонентов (Debezium WAL, CDN origin fetch) — doc/2.08
. (+) Добавлена секция "Метрики хранилищ" в Observability: 7 метрик (Redis hit rate, Redis memory, PG replication lag, PG connections, Kafka consumer lag, S3 errors, Debezium CDC lag) с порогами P1/P2 — doc/2.09

== ДЗ 5
2026.04.06

. (+) Создана модель угроз для Baseline/Alpha: 3 вектора атаки (компрометация API-ключа, утечка артефактов S3, эскалация привилегий) с контрмерами — doc/3.01
. (+) Спроектирована аутентификация: API Keys (HMAC-SHA256, Redis cache TTL 5 мин, zero-plaintext хранение) + OAuth 2.0/OIDC (Authorization Code + PKCE, JWT RS256, JWKS cache 1h, access TTL 15 мин) — doc/3.01
. (+) Создана sequence-диаграмма аутентификации: M2M (API Key через Redis/PG), Human (OAuth 2.0 через IdP), Token Refresh flow — diagrams/security-auth-flow.puml → PNG
. (+) Спроектирована авторизация RBAC: матрица 7 ролей × 8 ресурсов (Models, Deployments, Inference, Batch, Metrics, Audit, Users/Keys, Quotas); Service Roles по принципу least privilege; OPA — extension point MVP — doc/3.01
. (+) Создана компонентная диаграмма Defence in Depth: 5 слоёв (WAF → API Gateway/Auth → RBAC → Service Layer → Data RLS) + Vault как cross-cutting concern — diagrams/security-rbac.puml → PNG
. (+) Выбран HashiCorp Vault: обоснован trade-off Vault vs K8s Secrets vs AWS Secrets Manager; схема dynamic secrets для PostgreSQL (TTL 1h), Vault Agent Sidecar, zero-downtime rotation (two-key overlap window) — doc/3.01
. (+) Создана диаграмма flow управления секретами: Pod startup → Vault Agent init → K8s Auth → Dynamic PG credentials → file inject → app inotify reload; TLS rotation через cert-manager + Vault PKI — diagrams/security-secrets-flow.puml → PNG
. (+) Спроектирована изоляция тенантов: PostgreSQL RLS по tenant_id, S3 prefix-based IAM, Redis key-space prefix, Kafka consumer group ACL; автоматический integration test изоляции в CI как gate check — doc/3.01
. (+) Описана TLS конфигурация: TLS 1.3 preferred, AEAD cipher suites (PFS), HSTS; mTLS — extension point MVP с обоснованием (K8s NetworkPolicy достаточен для Baseline) — doc/3.01
. (+) Создан раздел "Аудит безопасности": архитектура audit-log (отдельный Loki stream, retention 1 год hot / 7 лет cold), JSON-схема события (12 полей), 11 типов событий, гарантии доставки at-least-once через PostgreSQL WAL + CDC — doc/3.02
. (+) Добавлен мониторинг аномального поведения: 7 метрик безопасности (auth_failures, authz_denied, inference anomaly, vault_access, tls_errors, key_rotation_overdue, tenant_isolation_violations) с порогами P0–P2, примеры Alertmanager правил — doc/3.02, doc/2.09
. (+) Созданы runbook'и для 3 security-инцидентов: компрометация API-ключа (RTO <5 мин), нарушение изоляции тенантов (P0, RTO <2 мин), истечение TLS-сертификата — doc/3.02
. (+) Добавлены Vault и OPA в таблицы паттернов и ключевых компонентов — doc/2.02
. (+) Раскомментирован include секции "Безопасность" в architecture-decision.adoc